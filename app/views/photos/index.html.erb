<div class="page-header"><h1>Photos</h1></div>

<%#= render @photos %>

<div class="row">
  <% if @photos %>
    <% @photos.each do |photo| %>
      <div class="col-md-2">
        <%= render 'photo', photo: photo %>
      </div>
    <% end %>
  <% end %>
</div>
