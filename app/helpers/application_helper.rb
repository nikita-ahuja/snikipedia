module ApplicationHelper

  # def authorized_user(author)
  #   current_user ||= User.find(session[:user_id]) if session[:user_id]
  #   # @current_user ||= User.find(session[:user_id])
  #   if current_user && (current_user == author || current_user.role == "admin")
  #     return true
  #   else
  #     return false
  #   end
  # end

end
